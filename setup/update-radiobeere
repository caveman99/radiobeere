#!/bin/bash

# Dynamisch ermitteltes Installationsverzeichnis
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
INSTALL_DIR="$(dirname "$SCRIPT_DIR")"

cd "$INSTALL_DIR"
git pull > /var/www/radiobeere-update.log 2>&1
